CREATE TABLE IF NOT EXISTS addresses (
    country text,
    state text,
    county text,
    city text,
    postal_code text,
    streed_address_line_1 text,
    streed_address_line_2 text,
    customer_uuid text
);